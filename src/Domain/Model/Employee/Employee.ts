import EmployeeID from "./EmployeeProfile/EmployeeID";
import EmployeeName from "./EmployeeProfile/EmployeeName";

export default class Employee {
  private id: EmployeeID;
  private firstName: EmployeeName;
  private lastName: EmployeeName;

  public changeName(firstName: EmployeeName) {
    this.name = name;
  }
}
